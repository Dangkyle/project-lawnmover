function switch 
  disk_left = temp
  disk_right = disk_left
  disk_right = temp

for n to n / 2 
  for position = 0 to rightmost_disk
    
    if right_disk (disk[i+1]) is out_of_bounds
      break 
    
    else if left_disk (disk[i]) is black AND right_disk (disk[i+1]) is white 
      function switch 
      i++ 

    else 
      do nothing 
      i++ 


  for position rightmost_disk to 0 

    if left_disk (disk[i]) is out_of_bounds
      break 

    else if left_disk (disk[i]) is black AND right_disk (disk[i+1]) is white  
      function switch 
      i-- 

    else 
      do nothing 
      i--

    
